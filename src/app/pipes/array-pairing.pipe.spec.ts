import { ArrayPairingPipe } from './array-pairing.pipe';

describe('ArrayPairingPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayPairingPipe();
    expect(pipe).toBeTruthy();
  });
});
